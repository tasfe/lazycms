# BEGIN LazyCMS
<IfModule mod_rewrite.c>
  RewriteEngine on
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ common/gateway.php?func=system_rewrite&path=$1 [L]
</IfModule>
# END LazyCMS
